import PageIntro from '../../components/PageIntro'
import PageShell from '../../components/PageShell'
import { caseStudies, projects, technologies } from '../../lib/portfolio'

export default function PortfolioPage() {
	return <PageShell><PageIntro eyebrow="Portfolio · projects and experience" title="Proof, in working order." body="A structured record of telecom, IoT, automation, embedded, education, and fabrication work carried forward from the previous portfolio." /><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><div className="flex flex-wrap gap-3 text-sm font-bold"><span className="rounded-full bg-ink px-4 py-2 text-white">25+ projects</span><span className="rounded-full border border-line bg-white px-4 py-2">10+ years experience</span><span className="rounded-full border border-line bg-white px-4 py-2">50+ clients</span><span className="rounded-full border border-line bg-white px-4 py-2">500+ students trained</span></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{projects.map((project) => <article key={project.slug} className="overflow-hidden rounded-2xl border border-line bg-white"><div className={`h-3 ${project.accent}`} /><div className="p-5"><p className="text-xs font-black uppercase tracking-widest text-cobalt">{project.type}</p><h2 className="mt-5 font-display text-2xl font-bold">{project.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{project.summary}</p><div className="mt-5 flex flex-wrap gap-2">{project.tags.map((tag) => <span key={tag} className="rounded-full bg-mist px-2.5 py-1 text-[10px] font-bold text-slate-500">{tag}</span>)}</div></div></article>)}</div><div className="mt-16"><p className="text-xs font-black uppercase tracking-[.24em] text-cobalt">Featured case studies</p><div className="mt-6 grid gap-4 lg:grid-cols-3">{caseStudies.map((study) => <article key={study.title} className="border-t-2 border-ink bg-white p-6"><p className="text-xs font-bold text-slate-500">{study.meta}</p><h2 className="mt-6 font-display text-2xl font-bold">{study.title}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{study.description}</p><ul className="mt-5 grid gap-2 text-sm font-bold">{study.results.map((result) => <li key={result}><span className="mr-2 text-signal">✓</span>{result}</li>)}</ul></article>)}</div></div><div className="mt-16 grid gap-4 border-y border-line py-10 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(technologies).map(([group, items]) => <div key={group}><p className="text-xs font-black uppercase tracking-widest text-cobalt">{group}</p><p className="mt-3 text-sm leading-7 text-slate-600">{items.join(' · ')}</p></div>)}</div></section></PageShell>
}
